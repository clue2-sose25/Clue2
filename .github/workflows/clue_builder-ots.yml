name: Build and Push OTS Image
on:
  push:
    branches: [ "dev", "main" ]
    paths:
      - 'clue_builders/ots/**'
  pull_request:
    branches: [ "dev" ]
    paths:
      - 'clue_builders/ots/**'

permissions:
  contents: read            # for checkout
  packages: write           # needed to push to GHCR

jobs:
  build_clue_ots:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ots
    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Lint Dockerfile (ots builder)
        run: docker run --rm -i hadolint/hadolint --ignore DL3059 --ignore DL3008 - < clue_builders/ots/Dockerfile
      - name: Compute short Git SHA
        id: vars
        run: |
          echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV
          echo "::set-output name=short_sha::${GITHUB_SHA::7}"
      - name: Build and push ots image
        if: ${{ github.event_name != 'pull_request' }}
        uses: docker/build-push-action@v3
        with:
          context: .
          file: clue_builders/ots/Dockerfile
          platforms: linux/amd64
          tags: |
            ghcr.io/clue2-sose25/clue2-${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.short_sha }}
            ghcr.io/clue2-sose25/clue2-${{ env.IMAGE_NAME }}:${{ github.ref_name }}
            ghcr.io/clue2-sose25/clue2-${{ env.IMAGE_NAME }}:pr-test
            ${{ github.ref_name == 'main' && format('ghcr.io/clue2-sose25/clue2-{0}:latest', env.IMAGE_NAME) || '' }}
          push: true
          cache-from: type=gha,scope=clue_ots
          cache-to: type=gha,scope=clue_ots,mode=max
      - name: Build ots image (no push, PR test)
        if: ${{ github.event_name == 'pull_request' }}
        uses: docker/build-push-action@v3
        with:
          context: .
          file: clue_builders/ots/Dockerfile
          platforms: linux/amd64
          tags: ghcr.io/clue2-sose25/clue2-${{ env.IMAGE_NAME }}:pr-test
          push: true
          load: true
          cache-from: type=gha,scope=clue_ots
          cache-to: type=gha,scope=clue_ots,mode=max
      - name: Smoke test ots container
        run: |
          docker run -d --name=test_clue_ots ghcr.io/clue2-sose25/clue2-${IMAGE_NAME}:pr-test /bin/sh -c "echo OTS OK"
          sleep 5
          docker logs test_clue_ots || true
          docker stop test_clue_ots
