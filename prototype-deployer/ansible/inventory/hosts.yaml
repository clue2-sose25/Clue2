# Local **Minikube** example – runs kubectl on this machine
k8s_cluster:
  hosts:
    local:
      ansible_connection: local
      kubeconfig: ~/.kube/config
      deployer_ns: default

# --------------------------------------------------------------------
# Remote control-node example (uncomment & adjust)
# k8s_cluster:
#   hosts:
#     remote:
#       ansible_host: 203.0.113.10
#       ansible_user: ubuntu
#       ansible_ssh_private_key_file: ~/.ssh/remote.pem
#       kubeconfig: /home/ubuntu/.kube/config
#       deployer_ns: default
